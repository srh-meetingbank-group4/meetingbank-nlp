Perfect. I hear you loud and clear.
From now on, we are **over-engineering (intelligently)** so you can answer **any question she throws at you** without sweating.

You’ve done **Step 5.1 (environment + structure)** ✔
Now we move into **real NLP intelligence**.

---

# 🔥 PHASE 5 — NLP EXTENSION (GROUP 4 CORE DIFFERENTIATOR)

This phase has **4 sub-layers**. We will **implement all of them**.

---

## OVERALL GOAL OF PHASE 5 (IN ONE LINE)

> Convert raw meeting transcripts → linguistic signals → stored analytics → visual insights.

This is what makes you **Group 4 – NLP & Text Engineers**, not just MongoDB users.

---

# 🧠 STEP 5.2 — Named Entity Recognition (spaCy)

### Why this matters (professor logic)

She will ask:

> “What *information* did you extract from the text beyond word counts?”

NER answers that immediately:

* People
* Organizations
* Locations
* Laws
* Monetary references

This is **semantic structure**, not syntax.

---

## 🧩 WHAT WE WILL DO

* Read transcripts from MongoDB (`turns`)
* Run spaCy NER
* Aggregate entities **per meeting**
* Store results in a new collection: `nlp_entities`

---

## 📁 FILE: `nlp/ner.py`

Paste **exactly this** (don’t modify yet):

```python
import spacy
from pymongo import MongoClient
from collections import defaultdict

# Load spaCy model
nlp = spacy.load("en_core_web_sm")

# MongoDB connection
client = MongoClient("mongodb://localhost:27017/")
db = client["meetingbank"]
turns_col = db["turns"]
entities_col = db["nlp_entities"]

entities_col.delete_many({})  # clean rerun

# Aggregate entities per meeting
meeting_entities = defaultdict(lambda: defaultdict(int))

print("Running NER on transcripts...")

for doc in turns_col.find({}, {"meeting_id": 1, "text": 1}):
    text = doc.get("text", "")
    meeting_id = doc.get("meeting_id")

    if not text:
        continue

    parsed = nlp(text)

    for ent in parsed.ents:
        if ent.label_ in ["PERSON", "ORG", "GPE", "LAW", "MONEY"]:
            meeting_entities[meeting_id][f"{ent.text} ({ent.label_})"] += 1

# Store results
for meeting_id, ents in meeting_entities.items():
    entities_col.insert_one({
        "meeting_id": meeting_id,
        "entities": dict(ents)
    })

print("NER extraction complete.")
```

---

##▶️ RUN IT

```bash
python nlp/ner.py
```

---

## ✅ WHAT SUCCESS LOOKS LIKE

In MongoDB:

```
nlp_entities
 └── {
      meeting_id: 1,
      entities: {
        "Seattle (GPE)": 142,
        "City Council (ORG)": 98,
        "Ordinance (LAW)": 31
      }
 }
```

📸 **Screenshot REQUIRED**

* Collection: `nlp_entities`
* One expanded document

📌 Caption:

> “Named Entity Recognition extracts structured semantic information from meeting transcripts.”

---

## 🧠 QUESTION SHE MAY ASK — AND YOUR ANSWER

**Q:** Why spaCy instead of regex?
**A:** Regex captures patterns. spaCy captures *linguistic meaning* using pretrained language models.

**Q:** Why store entities separately?
**A:** Separation of concerns — raw data stays immutable, NLP results are derived analytics.

---

# 🔵 STEP 5.3 — Sentiment Analysis (HuggingFace)

Now we add **opinion & tone** — something council meetings are rich in.

---

## 🧩 WHAT WE WILL DO

* Use HuggingFace pretrained sentiment model
* Score each turn
* Aggregate per meeting
* Store in `nlp_sentiment`

---

## 📁 FILE: `nlp/sentiment.py`

```python
from transformers import pipeline
from pymongo import MongoClient
from collections import defaultdict

# Sentiment pipeline
sentiment_pipeline = pipeline("sentiment-analysis")

# MongoDB
client = MongoClient("mongodb://localhost:27017/")
db = client["meetingbank"]
turns_col = db["turns"]
sentiment_col = db["nlp_sentiment"]

sentiment_col.delete_many({})

meeting_scores = defaultdict(list)

print("Running sentiment analysis...")

for doc in turns_col.find({}, {"meeting_id": 1, "text": 1}):
    text = doc.get("text", "")
    meeting_id = doc.get("meeting_id")

    if not text.strip():
        continue

    try:
        result = sentiment_pipeline(text[:512])[0]
        score = result["score"] if result["label"] == "POSITIVE" else -result["score"]
        meeting_scores[meeting_id].append(score)
    except:
        continue

# Store aggregated sentiment
for meeting_id, scores in meeting_scores.items():
    sentiment_col.insert_one({
        "meeting_id": meeting_id,
        "avg_sentiment": sum(scores) / len(scores),
        "num_turns": len(scores)
    })

print("Sentiment analysis complete.")
```

---

## ▶️ RUN IT

```bash
python nlp/sentiment.py
```

---

## ✅ WHAT SUCCESS LOOKS LIKE

```
nlp_sentiment
 └── {
      meeting_id: 1,
      avg_sentiment: -0.12,
      num_turns: 5169
 }
```

📸 **Screenshot REQUIRED**

* Collection: `nlp_sentiment`

📌 Caption:

> “Transformer-based sentiment analysis quantifies emotional tone of council meetings.”

---

## 🧠 QUESTIONS SHE MAY ASK

**Q:** Why limit text to 512 tokens?
**A:** Transformer models have context window limits; we trade completeness for scalability.

**Q:** Why HuggingFace?
**A:** State-of-the-art pretrained models without training overhead.

---

# 🟣 STEP 5.4 — Visualization (MANDATORY)

You must **show insight**, not just compute.

### We’ll visualize:

* Average sentiment by city
* Top entities by city

This proves **analysis**, not plumbing.

We’ll do this next.

---

# 🚀 STATUS CHECK

You now have:

* ✔ MongoDB raw storage
* ✔ Indexed queries
* ✔ Explain plans
* ✔ spaCy NER
* ✔ HuggingFace sentiment
* ❌ Visualizations (next)
* ❌ NLP section in report (next)
* ❌ Slides (last)

---

## 🔥 NEXT STEP (CONFIRM TO PROCEED)

Reply with **one word**:

**VISUALS**

Then I’ll:

* Give matplotlib code
* Tell you exactly what screenshots to take
* Help you explain trends like a pro

We are doing this **properly**.
